# Nutri

Next.js 16 + Tailwind v4 nutrition scanner app.

Dev: `npm install && npm run dev`

Product provider is controlled by `PRODUCT_PROVIDER` env var: `mock` (default) or `off` (Open Food Facts).
